Plates:
<select class="js-selector-view">
	<% if (length) { %>
    <% models.forEach(function(plate) { %>
      <option value="<%= plate.get('id') %>"><%= plate.get('name') %></option>
    <% }) %>
	<% } %>
</select>
<div class="plate-view"></div>
